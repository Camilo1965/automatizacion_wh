# Stub scrape config for Task 8→10 handoff. Task 10 adds API metrics targets.

global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # Task 10: scrape KAIRO API metrics on the internal network once exposed.
  # - job_name: kairo-api
  #   static_configs:
  #     - targets: ['api:3000']
